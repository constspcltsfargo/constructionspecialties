
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow anyone to read files. This is safe for public assets like gallery images.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null; // Keep writes restricted to logged-in users.
    }
  }
}
