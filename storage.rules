
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read and write access.
      // This is a permissive rule for development.
      // For production, you must implement more granular security.
      allow read, write;
    }
  }
}

