
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's public profile information.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The user's full name."
        },
        "username": {
          "type": "string",
          "description": "The user's unique username."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "photoURL": {
            "type": "string",
            "format": "uri",
            "description": "URL of the user's profile picture."
        },
        "role": {
          "type": "string",
          "description": "The user's role (e.g., 'admin', 'user').",
          "enum": ["admin", "user"]
        }
      },
      "required": ["name", "username", "email"]
    },
    "Page": {
      "title": "Page",
      "type": "object",
      "description": "Represents a page on the website, allowing for CMS-style editing. Can be a simple page with a slug and HTML content, or a structured page like the homepage.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Page entity. For the homepage, this is 'home'."
        },
        "title": {
          "type": "string",
          "description": "The title of the page (for simple pages)."
        },
        "slug": {
          "type": "string",
          "description": "The URL-friendly slug for the page (e.g., 'about-us')."
        },
        "content": {
          "type": "string",
          "description": "The HTML or JSON content of the page. For the homepage, this will be a JSON object with structured content."
        }
      },
      "required": [
        "id"
      ]
    },
    "Media": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Media",
      "type": "object",
      "description": "Represents a media file stored in the storage bucket.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Media entity."
        },
        "filename": {
          "type": "string",
          "description": "The original filename of the media file."
        },
        "url": {
          "type": "string",
          "description": "The URL where the media file can be accessed.",
          "format": "uri"
        },
        "mimeType": {
          "type": "string",
          "description": "The MIME type of the media file (e.g., 'image/jpeg')."
        },
        "size": {
          "type": "number",
          "description": "The size of the media file in bytes."
        },
        "uploadDate": {
          "type": "string",
          "description": "The date and time the media file was uploaded.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "filename",
        "url",
        "mimeType",
        "size",
        "uploadDate"
      ]
    },
    "PageElement": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PageElement",
      "type": "object",
      "description": "Represents a reusable element that can be dragged and dropped onto a page.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the PageElement entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the element (e.g., 'Hero Section', 'Contact Form')."
        },
        "type": {
          "type": "string",
          "description": "The type of element (e.g., 'text', 'image', 'video', 'form')."
        },
        "content": {
          "type": "string",
          "description": "The content of the element, which could be HTML or JSON data."
        },
        "pageId": {
          "type": "string",
          "description": "Reference to Page. (Relationship: Page 1:N PageElement)"
        },
        "order": {
          "type": "number",
          "description": "The order in which the element appears on the page."
        }
      },
      "required": [
        "id",
        "name",
        "type",
        "content",
        "pageId",
        "order"
      ]
    },
    "EstimateRequest": {
        "title": "Estimate Request",
        "description": "Represents a submission from the contact/estimate form.",
        "type": "object",
        "properties": {
          "name": { "type": "string", "description": "Full name of the requester." },
          "email": { "type": "string", "format": "email" },
          "phone": { "type": "string" },
          "zip": { "type": "string" },
          "project": { "type": "string", "description": "The project description/message." },
          "howDidYouHear": { "type": "string" },
          "submittedAt": { "type": "string", "format": "date-time" },
          "suggestedTeam": { "type": "string", "description": "AI-suggested team for routing." },
          "summary": { "type": "string", "description": "AI-generated summary of the request." },
          "nearbyBranches": {
            "type": "array",
            "items": { "type": "string" },
            "description": "AI-suggested nearby branches based on location."
          }
        },
        "required": ["name", "email", "phone", "zip", "project", "submittedAt"]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": { "$ref": "#/backend/entities/UserProfile" },
          "description": "Stores public user profile information. Users can read all profiles but can only write to their own.",
          "params": [{ "name": "userId", "description": "The unique identifier for the user, corresponding to their Firebase Auth UID." }]
        }
      },
      {
        "path": "/pages/{pageId}",
        "definition": {
          "entityName": "Page",
          "schema": { "$ref": "#/backend/entities/Page" },
          "description": "Stores page content for the website. Accessible and editable only by admins.",
          "params": [{ "name": "pageId", "description": "The unique identifier for the page." }]
        }
      },
      {
        "path": "/pages/{pageId}/pageElements/{pageElementId}",
        "definition": {
          "entityName": "PageElement",
          "schema": { "$ref": "#/backend/entities/PageElement" },
          "description": "Stores reusable elements associated with a specific page. Accessible and editable only by admins.",
          "params": [
            { "name": "pageId", "description": "The unique identifier for the page." },
            { "name": "pageElementId", "description": "The unique identifier for the page element." }
          ]
        }
      },
      {
        "path": "/media/{mediaId}",
        "definition": {
          "entityName": "Media",
          "schema": { "$ref": "#/backend/entities/Media" },
          "description": "Stores media files. Accessible and editable only by admins.",
          "params": [{ "name": "mediaId", "description": "The unique identifier for the media file." }]
        }
      },
      {
        "path": "/roles_admin/{userId}",
        "definition": {
          "entityName": "roles_admin",
          "schema": { "$ref": "#/backend/entities/roles_admin" },
          "description": "Marks a user as an administrator. The first authenticated user can create their own admin document if the collection is empty. Subsequent admin roles can only be granted by an existing admin.",
          "params": [{ "name": "userId", "description": "The unique identifier for the user." }]
        }
      },
      {
        "path": "/estimateRequests/{requestId}",
        "definition": {
            "entityName": "EstimateRequest",
            "schema": { "$ref": "#/backend/entities/EstimateRequest" },
            "description": "Stores all contact form submissions for estimates. Only accessible by admins.",
            "params": [{ "name": "requestId", "description": "The unique identifier for the estimate request." }]
        }
      }
    ],
    "reasoning": "This design prioritizes security and scalability. The `estimateRequests` collection is added to store form submissions and is restricted to admin access only, ensuring customer data privacy."
  }
}
